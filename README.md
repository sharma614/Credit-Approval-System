API Endpoints
Base URL: http://localhost:8000​

All endpoints are CSRF‑exempt and return JSON responses for easy testing in Postman.​

IDs (customer_id, loan_id) are auto-generated by the system where applicable; do not include them in create requests unless specified by the endpoint.​

Register Customer
Method: POST​
URL: /api/register/
Headers: Content-Type: application/json​
Request body: first_name, last_name, age, monthly_income, phone_number​
Example request body:
{
"first_name": "John",
"last_name": "Doe",
"age": 30,
"monthly_income": 60000,
"phone_number": 9876543210
}
Success: Returns customer details with auto-generated customer_id, computed approved_limit = 36 × monthly_income rounded to nearest lakh.​
Note: Do not pass customer_id in the request.​

Check Loan Eligibility
Method: POST​
URL: /api/check-eligibility/​
Headers: Content-Type: application/json​
Request body: customer_id, loan_amount, interest_rate, tenure​
Example request body:
{
"customer_id": 302,
"loan_amount": 500000,
"interest_rate": 10,
"tenure": 24
}
Success: Returns approval (bool), corrected_interest_rate (may adjust to meet slab), monthly_installment (EMI), and credit_score as per rules.​
Notes: Eligibility enforces slabs (>50 approve; 30–50 needs ≥12%; 10–30 needs ≥16%; <10 reject) and affordability (sum of all EMIs ≤ 50% of monthly salary).​

Create Loan
Method: POST​
URL: /api/create-loan/​
Headers: Content-Type: application/json​
Request body: customer_id, loan_amount, interest_rate, tenure​
Example request body:
{
"customer_id": 302,
"loan_amount": 500000,
"interest_rate": 10,
"tenure": 24
}
Success: Returns loan_approved flag, auto-generated loan_id on approval, monthly_installment, and message when not approved.​
Note: Do not pass loan_id in the request; it is assigned on approval.​

View Single Loan
Method: GET​
URL: /api/view-loan/{loan_id}/​
Example: /api/view-loan/9997/​
Success: Returns loan details including nested customer object with id, first_name, last_name, phone_number, age, plus loan_amount, interest_rate, monthly_installment, and tenure.​

View Customer Loans
Method: GET​
URL: /api/view-loans/{customer_id}/​
Example: /api/view-loans/302/​
Success: Returns a list of loans for the customer with loan_id, loan_amount, interest_rate, monthly_installment, and repayments_left.​

Admin Interface
Method: GET​
URL: /admin/​
Note: This is the Django Admin web UI for managing records and is not a JSON API.​

Sample Test Flow (Postman)
Register customer via POST /api/register/ using the provided body fields and capture the returned customer_id for subsequent requests.​
Check eligibility via POST /api/check-eligibility/ with customer_id, loan_amount, interest_rate, and tenure to view approval decision and any corrected_interest_rate.​
Create loan via POST /api/create-loan/ only if eligibility approval is true to persist the loan and receive loan_id and monthly_installment.​
View loan via GET /api/view-loan/{loan_id}/ to confirm persisted loan details and structure.​
View all loans via GET /api/view-loans/{customer_id}/ to list the customer’s active and historical loans with repayments_left.​

Error Responses
Invalid customer_id or loan_id returns {"error": "Customer/Loan not found"} with appropriate HTTP status codes for resource absence.​
Validation failures return field-level errors describing missing or invalid inputs as JSON for quick debugging in Postman.​
Business rule failures, such as EMI affordability or credit score slab mismatch, are surfaced with explanatory messages and approval=false where applicable
